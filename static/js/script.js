var options = {
  strings: ['SoftWare Developer', 'Open source enthusiastic'],
  typeSpeed: 100,
  backSpeed: 100,
  smartBackspace: true,
  loop: true,
};

var typed = new Typed('.typed-out-word', options);